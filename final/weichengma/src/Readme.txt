In the filter directory:
-DictBuild turns input file into certain format for loading the dictionary
-RestaurantDict builds a global dictionary for storing restaurant names

In the test directory:
-TestMain serves as the entrance of program, combining input from different sources, building dictionary, and turn them into a final output file.

Directories input, dict, source and dest stores source files and output files.
